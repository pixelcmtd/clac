// TODO: unicode compliance
WHITESPACE = _{ " " | NEWLINE }

// TODO: unicode support
id = { ASCII_ALPHANUMERIC }
ids = _{ id+ }

expr = _{ func | ("(" ~ expr ~ ")") | (id ~ expr) | id }

params = { ids }
body = { expr }
func = { ("λ" | "Λ") ~ params ~ "." ~ body }
