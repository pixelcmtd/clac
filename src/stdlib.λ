I ← λa.a
K ⇐ λab.a
KI ⇐ KI
S ⇐ λfgx.fx(gx)
C ⇐ λfxy.fyx
B ⇐ λfgx.f(gx)
M ⇐ λf.ff
Ω ⇐ MM

id ← I
const ← K
flip ← C

T ∈ Bool
T ← K
F ∈ Bool
F ← KI
! ∈ Bool → Bool
! ← C
∧ ∈ Bool → Bool → Bool
∧ ⇐ λpq.pqp
∨ ∈ Bool → Bool → Bool
∨ ← M

+ ∈ Int → Int → a → b
+ ⇐ λmnfx.mf(nfx)
# should infer to be Int → a → b
succ ← + 1
# TODO: rethink whether to use something like \cdot instead
* ← B
